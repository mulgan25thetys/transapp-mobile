import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'transfert.tncf',
  appName: 'transappMobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
